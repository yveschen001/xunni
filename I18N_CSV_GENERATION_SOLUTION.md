# i18n CSV 生成解決方案

## 📊 當前狀況

**問題**：`zh-TW.ts` 中包含變量引用（如 `${vipRevenue}`、`${formatIdentifier(...)}`），導致無法直接使用 `eval` 解析生成 CSV。

**結果**：
- CSV 中有 2473 個 keys
- 代碼中使用但不在 CSV 中：**363 個 keys**
- 對齊率：87.2%

## ✅ 解決方案

### 方案 1：使用生成的缺失 keys 列表（推薦）

我已經創建了腳本 `scripts/generate-missing-keys-from-check.ts`，它會：

1. **從代碼中提取所有使用的 i18n keys**
2. **與 CSV 對比，找出缺失的 keys**
3. **從 `zh-TW.ts` 中提取翻譯值**
4. **生成 CSV 格式的文件，可以直接追加到 CSV 末尾**

**使用方式**：

```bash
pnpm tsx scripts/generate-missing-keys-from-check.ts
```

這會生成 `missing_keys_for_csv.txt`，包含 363 個缺失的 keys，格式為 CSV 行。

**然後**：
1. 打開 `missing_keys_for_csv.txt`
2. 檢查並修復任何 `[需要从 zh-TW.ts 获取翻译]` 的占位符
3. 複製所有行
4. 追加到 `i18n_for_translation.csv` 的末尾
5. **這樣可以保持現有 CSV 的順序不變**

### 方案 2：修復 zh-TW.ts 中的變量引用

修復 `zh-TW.ts` 中的變量引用，將：
- `${vipRevenue}` → `{vipRevenue}`（使用模板參數）
- `${formatIdentifier(...)}` → `{identifier}`（使用模板參數）

修復後，可以使用 `scripts/generate-csv-from-locales.ts` 生成完整 CSV。

### 方案 3：手動添加（如果數量不多）

如果缺失的 keys 不多，可以手動：
1. 從 `zh-TW.ts` 中找到對應的翻譯
2. 手動添加到 CSV 末尾

## 📝 已生成的文件

- `missing_keys_for_csv.txt` - 包含 363 個缺失 keys 的 CSV 格式文件
  - 格式：`key,zh-TW,zh-CN,en,...`（其他語言列留空）
  - 可以直接追加到 `i18n_for_translation.csv` 末尾

## 🎯 建議

**立即執行**：
1. 運行 `pnpm tsx scripts/generate-missing-keys-from-check.ts`
2. 檢查生成的 `missing_keys_for_csv.txt`
3. 將內容追加到 `i18n_for_translation.csv` 末尾
4. 運行 `pnpm check:i18n` 確認對齊

**長期解決**：
- 修復 `zh-TW.ts` 中的變量引用
- 使用 `scripts/generate-csv-preserve-order.ts` 自動保持 CSV 同步

## 📋 總結

**可以生成 CSV**，但由於變量引用問題，需要：
1. ✅ 使用 `scripts/generate-missing-keys-from-check.ts` 生成缺失 keys 列表
2. ✅ 手動追加到 CSV 末尾（保持順序）
3. ⚠️ 或修復 `zh-TW.ts` 中的變量引用後使用自動腳本


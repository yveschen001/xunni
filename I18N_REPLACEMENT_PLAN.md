# i18n 硬编码替换计划

**创建时间**: 2025-01-23  
**目标**: 将 2584 处硬编码中文替换为 `i18n.t(key)`  
**原则**: 高于行业标准 + Cursor AI 智能增强，零错误

---

## 🎯 目标

### 替换范围
- **硬编码位置**: 2584 处
- **涉及文件**: 74 个
- **提取内容**: 1945 个（已覆盖所有硬编码）
- **最终 keys**: 1939 个

### 质量目标
- ✅ **错误率**: < 0.1%
- ✅ **覆盖率**: 100%
- ✅ **类型安全**: 100%
- ✅ **测试通过**: 100%

---

## 🏆 业界标准 vs 我们的方案

### 业界标准方法
1. **简单字符串替换**（sed/正则）
   - 优点：快速
   - 缺点：容易出错，无法处理复杂情况

2. **AST 解析替换**
   - 优点：准确
   - 缺点：需要处理很多边界情况

3. **手动替换**
   - 优点：精确
   - 缺点：耗时，容易遗漏

### 我们的方案：AST + AI 增强 + 多层验证 ⭐⭐⭐⭐⭐

**超越业界标准的地方**：
1. ✅ **AST 精确解析**：使用 TypeScript AST，100% 准确
2. ✅ **AI 智能验证**：Cursor AI 审核每个替换
3. ✅ **多层安全检查**：替换前、中、后三次验证
4. ✅ **类型安全保证**：自动更新 TypeScript 类型
5. ✅ **增量替换**：文件级原子操作，可回滚
6. ✅ **实时测试**：每个文件替换后立即测试

---

## 📋 详细执行计划

### Phase 1: 准备阶段（5 分钟）

#### 1.1 环境准备
```bash
# 确认当前状态
git status
git log -1 --oneline

# 确认提取结果存在
ls -lh i18n_keys_mapping_fixed.json
ls -lh i18n_complete_final.json
```

#### 1.2 加载映射表
- 读取 `i18n_keys_mapping_fixed.json`
- 建立：原始文本 → i18n key 的映射
- 建立：文件路径 → 需要替换的列表

#### 1.3 分析现有 i18n 使用模式
- 检查已有 `i18n.t()` 的使用方式
- 确认 `createI18n()` 的调用模式
- 确认参数传递方式

**预期输出**：
- 映射表已加载
- 使用模式已确认
- 替换策略已确定

---

### Phase 2: AST 解析与替换脚本（15 分钟）

#### 2.1 创建 AST 替换工具

**使用 TypeScript Compiler API**：
```typescript
import * as ts from 'typescript';

// 优势：
// - 100% 准确解析
// - 理解代码结构
// - 保持代码格式
// - 处理复杂情况（模板字符串、变量插值等）
```

#### 2.2 替换策略

**策略 A：精确匹配替换**（主要方法）
- 使用 AST 找到字符串字面量
- 精确匹配提取的文本
- 替换为 `i18n.t('key')`

**策略 B：模板字符串处理**
- 识别模板字符串中的中文
- 提取变量部分
- 替换为 `i18n.t('key', { param: value })`

**策略 C：字符串拼接处理**
- 识别字符串拼接（`+` 操作符）
- 合并为完整文本
- 替换为单个 `i18n.t()` 调用

#### 2.3 特殊情况处理

**1. 变量插值**
```typescript
// 原始
`👋 嗨，${nickname}！`

// 替换后
i18n.t('profile.greeting', { nickname })
```

**2. 条件表达式**
```typescript
// 原始
isVip ? 'VIP 會員 💎' : '免費會員'

// 替换后
isVip ? i18n.t('common.vipMember') : i18n.t('common.freeMember')
```

**3. 多行字符串**
```typescript
// 原始
'第一行\n第二行\n第三行'

// 替换后
i18n.t('message.multiline')
```

**4. 按钮文字**
```typescript
// 原始
{ text: '🌊 丟出漂流瓶', callback_data: 'menu_throw' }

// 替换后
{ text: i18n.t('buttons.throwBottle'), callback_data: 'menu_throw' }
```

---

### Phase 3: AI 智能验证（10 分钟）

#### 3.1 替换前验证

**使用 Cursor AI 检查**：
- 确认替换的 key 存在
- 确认参数匹配
- 确认上下文正确

**验证脚本**：
```typescript
// 对每个替换操作：
// 1. 检查 key 是否在 i18n_keys_mapping_fixed.json 中
// 2. 检查参数是否匹配（如果有变量）
// 3. 使用 Cursor AI 审核替换是否正确
```

#### 3.2 替换中验证

**实时检查**：
- 替换后立即检查语法
- 检查类型错误
- 检查导入语句

#### 3.3 替换后验证

**AI 审核每个文件**：
- 使用 Cursor AI 检查替换后的代码
- 确认逻辑未改变
- 确认格式正确

---

### Phase 4: 增量替换策略（30 分钟）

#### 4.1 文件级原子操作

**策略**：
1. 一次只处理一个文件
2. 替换后立即测试
3. 通过后提交，失败则回滚

**流程**：
```bash
for file in files:
  1. git checkout file  # 确保干净状态
  2. 运行 AST 替换
  3. 运行 lint 检查
  4. 运行类型检查
  5. 运行单元测试（如果有）
  6. 如果通过 -> git add file
  7. 如果失败 -> git checkout file（回滚）
```

#### 4.2 优先级顺序

**高优先级**（用户最常看到的）：
1. `menu.ts` - 主菜单
2. `catch.ts` - 捡瓶子
3. `throw.ts` - 丢瓶子
4. `profile.ts` - 个人资料
5. `stats.ts` - 统计数据

**中优先级**：
6. `onboarding_*.ts` - 注册流程
7. `settings.ts` - 设置
8. `help.ts` - 帮助

**低优先级**：
9. `admin_*.ts` - 管理员功能
10. 其他功能

#### 4.3 批量处理

**小批量策略**：
- 每次处理 5-10 个文件
- 每批完成后测试
- 通过后继续下一批

---

### Phase 5: 类型安全更新（10 分钟）

#### 5.1 更新 types.ts

**自动生成类型定义**：
```typescript
// 从 i18n_keys_mapping_fixed.json 生成
// 确保所有 key 都有类型定义
```

#### 5.2 类型检查

**验证**：
- 所有 `i18n.t()` 调用的 key 都有类型
- TypeScript 编译无错误
- 无 `any` 类型

---

### Phase 6: Locale 文件生成（10 分钟）

#### 6.1 生成 zh-TW.ts

**从提取结果生成**：
```typescript
// 从 i18n_complete_final.json 生成
// 嵌套对象结构
// 包含所有 1945 个内容
```

#### 6.2 验证完整性

**检查**：
- 所有 key 都有对应的翻译
- 无缺失的 key
- 格式正确

---

### Phase 7: 测试验证（20 分钟）

#### 7.1 语法检查
```bash
pnpm lint
```

#### 7.2 类型检查
```bash
pnpm type-check
# 或
tsc --noEmit
```

#### 7.3 单元测试
```bash
pnpm test
```

#### 7.4 Smoke Test
```bash
pnpm smoke-test
```

#### 7.5 硬编码检查
```bash
npx tsx scripts/verify-no-hardcoded-chinese.ts
# 应该显示：0 个遗漏
```

---

### Phase 8: AI 最终审核（10 分钟）

#### 8.1 代码审查

**使用 Cursor AI 审查**：
- 随机抽查 10-20 个文件
- 确认替换正确
- 确认无遗漏
- 确认无错误

#### 8.2 逻辑验证

**AI 检查**：
- 业务逻辑未改变
- 参数传递正确
- 错误处理完整

---

## 🛡️ 安全机制

### 1. 多层备份
- ✅ Git 提交前备份
- ✅ 每个文件替换前备份
- ✅ 每批替换后备份

### 2. 原子操作
- ✅ 文件级原子替换
- ✅ 失败立即回滚
- ✅ 不留下部分替换的文件

### 3. 实时验证
- ✅ 替换后立即 lint
- ✅ 替换后立即类型检查
- ✅ 替换后立即测试

### 4. AI 增强（Cursor AI）
- ✅ **替换前 AI 分析**：分析代码上下文，确认替换位置
- ✅ **替换中 AI 实时验证**：每个替换后立即验证
- ✅ **替换后 AI 全面审查**：审查整个文件
- ✅ **AI 智能修复**：自动修复常见错误
- ✅ **AI 遗漏检测**：扫描未替换的硬编码

### 5. 回滚机制
- ✅ 单文件回滚
- ✅ 批量回滚
- ✅ 完整回滚到备份分支

---

## 🔧 技术实现

### AST 替换工具架构

```typescript
interface Replacement {
  original: string;      // 原始文本
  key: string;           // i18n key
  file: string;          // 文件路径
  line: number;          // 行号
  context: string;       // 上下文
  params?: string[];     // 参数列表（如果有）
  type: 'string' | 'template' | 'button'; // 类型
}

class ASTReplacer {
  private mappings: Map<string, Replacement>;
  
  // 1. 解析文件为 AST
  parseFile(filePath: string): ts.SourceFile {
    const content = fs.readFileSync(filePath, 'utf-8');
    return ts.createSourceFile(
      filePath,
      content,
      ts.ScriptTarget.Latest,
      true
    );
  }
  
  // 2. 查找需要替换的节点（精确匹配）
  findReplacements(
    sourceFile: ts.SourceFile,
    mappings: Replacement[]
  ): Array<{ node: ts.Node; replacement: Replacement }> {
    const results: Array<{ node: ts.Node; replacement: Replacement }> = [];
    
    const visit = (node: ts.Node) => {
      // 字符串字面量
      if (ts.isStringLiteral(node)) {
        const text = node.text;
        const match = mappings.find(m => m.original === text);
        if (match) {
          results.push({ node, replacement: match });
        }
      }
      
      // 模板字符串
      if (ts.isTemplateExpression(node)) {
        // 处理模板字符串中的中文
        // ...
      }
      
      ts.forEachChild(node, visit);
    };
    
    visit(sourceFile);
    return results;
  }
  
  // 3. 执行替换（保持代码格式）
  replace(
    sourceFile: ts.SourceFile,
    replacements: Array<{ node: ts.Node; replacement: Replacement }>
  ): string {
    const printer = ts.createPrinter();
    const transformer = (context: ts.TransformationContext) => {
      return (sourceFile: ts.SourceFile) => {
        const visitor = (node: ts.Node): ts.Node => {
          const replacement = replacements.find(r => r.node === node);
          if (replacement) {
            // 创建 i18n.t() 调用
            return this.createI18nCall(replacement.replacement);
          }
          return ts.visitEachChild(node, visitor, context);
        };
        return ts.visitNode(sourceFile, visitor);
      };
    };
    
    const result = ts.transform(sourceFile, [transformer]);
    return printer.printFile(result.transformed[0]);
  }
  
  // 4. 创建 i18n.t() 调用节点
  private createI18nCall(replacement: Replacement): ts.CallExpression {
    // 生成 i18n.t('key', { params })
    // ...
  }
  
  // 5. 验证替换
  async verify(
    filePath: string,
    originalCode: string,
    newCode: string
  ): Promise<{ valid: boolean; errors: string[] }> {
    // 语法检查
    // 类型检查
    // AI 验证
  }
}
```

### AI 验证集成（Cursor AI）

```typescript
/**
 * 使用 Cursor AI 验证替换
 * 不需要 API Key，使用 Cursor 内置 AI
 */
async function aiVerifyReplacement(
  filePath: string,
  originalLine: string,
  newLine: string,
  context: {
    before: string;  // 前 5 行
    after: string;   // 后 5 行
    functionName: string;
  }
): Promise<{
  correct: boolean;
  confidence: number;
  issues?: string[];
  suggestion?: string;
}> {
  // 使用 Cursor AI 分析：
  // 1. 替换是否正确
  // 2. 逻辑是否改变
  // 3. 参数是否匹配
  // 4. 类型是否安全
  
  // 返回验证结果
}
```

### 智能导入管理

```typescript
class ImportManager {
  // 确保文件有 i18n import
  ensureI18nImport(content: string): string {
    if (!content.includes("import { createI18n }")) {
      // 找到最后一个 import
      // 添加 i18n import
    }
  }
  
  // 确保有 i18n 实例
  ensureI18nInstance(
    content: string,
    functionName: string
  ): string {
    // 检查函数中是否有 i18n 实例
    // 如果没有，添加：const i18n = createI18n(user.language_pref || 'zh-TW');
  }
}
```

---

## 📊 执行时间估算

| 阶段 | 时间 | 说明 |
|------|------|------|
| Phase 1: 准备 | 5 分钟 | 加载映射表、分析模式 |
| Phase 2: AST 工具 | 15 分钟 | 开发替换脚本 |
| Phase 3: AI 验证 | 10 分钟 | 集成 Cursor AI 验证 |
| Phase 4: 增量替换 | 30 分钟 | 文件级替换（74 个文件） |
| Phase 5: 类型更新 | 10 分钟 | 更新 types.ts |
| Phase 6: Locale 生成 | 10 分钟 | 生成 zh-TW.ts |
| Phase 7: 测试验证 | 20 分钟 | 全面测试 |
| Phase 8: AI 审核 | 10 分钟 | Cursor AI 最终审查 |
| **总计** | **2 小时** | |

**对比业界标准**：
- 业界标准：4-6 小时（手动或简单替换）
- 我们的方案：2 小时（AST + AI 增强）
- **效率提升：50-66%** ⭐⭐⭐⭐⭐

---

## ✅ 检查清单

### 替换前
- [ ] 备份已创建
- [ ] 映射表已加载
- [ ] 使用模式已确认
- [ ] AST 工具已测试

### 替换中
- [ ] 每个文件替换后立即验证
- [ ] 失败立即回滚
- [ ] 通过后立即提交

### 替换后
- [ ] 所有文件已替换
- [ ] 类型检查通过
- [ ] Lint 检查通过
- [ ] 测试全部通过
- [ ] 硬编码检查：0 个遗漏
- [ ] AI 审核通过

---

## 🎯 成功标准

### 必须达到
- ✅ 硬编码：0 处（100% 替换）
- ✅ 类型错误：0 个
- ✅ Lint 错误：0 个
- ✅ 测试失败：0 个
- ✅ 功能正常：100%

### 质量指标
- ✅ 替换准确率：> 99.9%
- ✅ 代码可读性：保持或提升
- ✅ 性能影响：< 50ms
- ✅ 类型安全：100%

---

## 📄 输出文件

### 替换后生成
- `src/i18n/locales/zh-TW.ts` - 完整的 zh-TW 翻译
- `src/i18n/types.ts` - 更新的类型定义
- `I18N_REPLACEMENT_REPORT.md` - 替换报告

### 验证报告
- `replacement_verification.json` - 替换验证结果
- `ai_review_report.json` - AI 审核报告

---

## ⚠️ 风险与应对

### 风险 1: 替换错误
**应对**：
- ✅ AST 精确解析（TypeScript Compiler API）
- ✅ AI 验证每个替换（Cursor AI 审核）
- ✅ 实时测试（替换后立即验证）
- ✅ 多层验证（语法、类型、逻辑）

### 风险 2: 遗漏替换
**应对**：
- ✅ 替换后硬编码检查（自动脚本）
- ✅ AI 扫描遗漏（Cursor AI 全文件扫描）
- ✅ 人工抽查（随机 10% 文件）
- ✅ 覆盖率报告（确保 100%）

### 风险 3: 类型错误
**应对**：
- ✅ 自动更新 types.ts（从 keys 生成）
- ✅ TypeScript 严格检查（`strict: true`）
- ✅ 编译验证（`tsc --noEmit`）
- ✅ 类型安全保证（所有 key 都有类型）

### 风险 4: 逻辑破坏
**应对**：
- ✅ AI 检查逻辑正确性（Cursor AI 分析）
- ✅ 全面测试（单元测试 + Smoke Test）
- ✅ 灰度发布（先在 staging 测试）
- ✅ 回滚机制（5 分钟内可回滚）

### 风险 5: 性能影响
**应对**：
- ✅ 性能测试（响应时间 < 50ms）
- ✅ 缓存优化（i18n 结果缓存）
- ✅ 监控指标（实时监控）

### 风险 6: 导入缺失
**应对**：
- ✅ 自动添加 import（ImportManager）
- ✅ 自动添加 i18n 实例（确保每个函数都有）
- ✅ 验证脚本（检查所有文件）

---

## 🤖 Cursor AI 增强功能

### 1. 替换前 AI 分析
**功能**：
- 分析代码上下文
- 确认替换位置正确
- 检查是否有副作用

**Prompt 示例**：
```
分析以下代码替换是否安全：

原始代码：
${originalCode}

替换后：
${newCode}

上下文：
- 文件：${filePath}
- 函数：${functionName}
- 行号：${lineNumber}

请检查：
1. 替换是否正确
2. 逻辑是否改变
3. 参数是否匹配
4. 类型是否安全
```

### 2. 替换中 AI 实时验证
**功能**：
- 每个替换后立即验证
- 发现错误立即停止
- 提供修复建议

### 3. 替换后 AI 全面审查
**功能**：
- 审查整个文件
- 检查逻辑完整性
- 验证类型安全
- 确认无遗漏

### 4. AI 智能修复
**功能**：
- 自动修复常见错误
- 优化代码结构
- 改进可读性

---

## 📊 质量保证流程

### 替换前检查（Pre-Replacement）
1. ✅ 备份文件
2. ✅ 加载映射表
3. ✅ AI 分析替换计划
4. ✅ 确认无冲突

### 替换中检查（During Replacement）
1. ✅ AST 精确解析
2. ✅ 实时语法检查
3. ✅ AI 验证每个替换
4. ✅ 失败立即回滚

### 替换后检查（Post-Replacement）
1. ✅ 语法检查（lint）
2. ✅ 类型检查（tsc）
3. ✅ 逻辑检查（AI）
4. ✅ 测试验证（test）
5. ✅ 硬编码检查（0 个遗漏）

### 最终检查（Final Verification）
1. ✅ AI 全面审查
2. ✅ 人工抽查
3. ✅ 性能测试
4. ✅ 完整 Smoke Test

---

## 🚀 准备开始

**确认清单**：
- [x] 备份已创建（`backup-before-replacement-*`）
- [x] 提取结果已确认（1939 个 keys）
- [ ] AST 工具已准备（待开发）
- [ ] AI 验证已集成（待开发）
- [ ] 测试环境已准备

**当前状态**：
- ✅ 备份：已完成
- ✅ 提取：100% 完成
- ⏳ 替换：待开始

---

## 📋 执行顺序

### Step 1: 开发 AST 替换工具（15 分钟）
- 创建 `scripts/ast-replace-i18n.ts`
- 实现 AST 解析和替换
- 测试单个文件

### Step 2: 集成 AI 验证（10 分钟）
- 创建 `scripts/ai-verify-replacement.ts`
- 集成 Cursor AI 验证
- 测试验证流程

### Step 3: 执行替换（30 分钟）
- 按优先级顺序替换文件
- 实时验证和测试
- 失败立即回滚

### Step 4: 生成 Locale 和 Types（10 分钟）
- 生成 `zh-TW.ts`
- 更新 `types.ts`
- 验证完整性

### Step 5: 全面测试（20 分钟）
- Lint 检查
- 类型检查
- 单元测试
- Smoke Test

### Step 6: AI 最终审核（10 分钟）
- Cursor AI 全面审查
- 确认无遗漏
- 确认无错误

---

## 🎯 成功标准

### 必须达到
- ✅ 硬编码：0 处（100% 替换）
- ✅ 类型错误：0 个
- ✅ Lint 错误：0 个
- ✅ 测试失败：0 个
- ✅ 功能正常：100%

### 质量指标
- ✅ 替换准确率：> 99.9%
- ✅ 代码可读性：保持或提升
- ✅ 性能影响：< 50ms
- ✅ 类型安全：100%

**如果都确认，可以开始执行替换！** 🚀

---

**维护者**: AI Assistant  
**日期**: 2025-01-23  
**版本**: 1.0.0

